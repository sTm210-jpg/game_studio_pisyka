using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pull_rain : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //System.Random random = new System.Random();
        //float x = random.Next(-200, 200);
        //float z = random.Next(-200, 200);
        //float hz = random.Next(-200, 200);
        //Instantiate(kaplya, new Vector3(x, 200, z), kaplya.transform.rotation);
        //kol_rain++;

        //if (kol_rain >= size_mass_rain)
        //{
        //    Destroy(GameObject.Find("Платформа(Clone)"));
        //    kol_rain--;
        //}
    }
}
